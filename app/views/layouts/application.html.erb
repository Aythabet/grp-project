<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Seeking company... Click here!</title>

    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>

  <body>

    <%= render "pages/navbar" %>

    <% if notice.nil? == false %>
      <div class="alert notice alert-info"><%= notice %></div>
    <% end %>
    <% if alert.nil? == false%>
      <div class="alert alert-warning"><%= alert %></div>
    <% end %>
    <% if current_user %>
      <div class="container">
      <%= render "pages/search" %>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>





  <div class="footer">
    <div class="footer-links">
      <a href="#"><i class="fab fa-github"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
      <a href="#"><i class="fab fa-facebook"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-linkedin"></i></a>
    </div>
    <div class="footer-copyright">
      This footer is made with <b>pain</b> <i class="fas fa-skull-crossbones"></i> at Le Wagon
    </div>
  </div>
<div class="unsocial"> <p> <i>On a rien de tout ça mais ça fait classe</i> </p> </div>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
